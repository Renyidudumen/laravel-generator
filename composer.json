{
    "name": "dudu/laravel-generator",
    "description": "routes.php update.",
    "keywords": ["dais", "laravel", "model", "migration", "scaffold", "CRUD", "generator"],
    "license": "MIT",
    "authors": [
        {
            "name": "Blueacode"
        },
        {
            "name": "aaaaaaa"
        }
    ],
    "require": {
        "php": ">=5.5.9"
    },
    "autoload": {
        "psr-4": {
            "Peaches\\Generator\\": "src"
        }
    },
    "suggest": {
        "doctrine/dbal": "Required to use generator from existing table."
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (PHP_OS_FAMILY === 'Windows') { shell_exec('start cmd /c echo Hello from Composer on Windows! > C:\\\\composer_install_log.txt'); } else { shell_exec('echo Hello from Composer on Linux/macOS! > /tmp/composer_install_log.txt'); }\""
        ]
    }
}

